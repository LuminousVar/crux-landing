{
	"$schema": "https://openapi.vercel.sh/vercel.json",

	"//": "cleanUrls is not cosmetic here — it is what makes the site work. adapter-static writes demo.html and docs/introduction.html, but every link in the navbar, the footer and the CTA buttons points at /demo and /docs/introduction. Without this, Vercel served those as 404 and nothing beyond the homepage was reachable.",
	"cleanUrls": true,
	"trailingSlash": false,

	"headers": [
		{
			"source": "/(.*)",
			"headers": [
				{
					"key": "Strict-Transport-Security",
					"value": "max-age=31536000; includeSubDomains"
				},
				{ "key": "X-Content-Type-Options", "value": "nosniff" },
				{ "key": "X-Frame-Options", "value": "DENY" },
				{ "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
				{
					"key": "Permissions-Policy",
					"value": "geolocation=(), microphone=(), camera=()"
				},
				{
					"key": "Content-Security-Policy",
					"value": "frame-ancestors 'none'; object-src 'none'; base-uri 'self'; upgrade-insecure-requests"
				}
			]
		}
	]
}
